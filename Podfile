# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FILMs' do
  pod 'Alamofire', '~> 5.2'
  pod 'SwiftyJSON', '~> 4.0'
  use_frameworks!

  # Pods for FILMs

end
